name: Propaganda Radar

on:
  schedule:
    # KST 기준: 09:05, 15:05, 21:05, 23:55
    # (GitHub cron은 UTC라서 UTC로 적음)
    - cron: "5 0 * * *"     # 09:05 KST
    - cron: "5 6 * * *"     # 15:05 KST
    - cron: "5 12 * * *"    # 21:05 KST
    - cron: "55 14 * * *"   # 23:55 KST (마지막 실행 - 텔레그램)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Decide SEND_TELEGRAM (last run only)
        id: decide
        run: |
          SEND="false"
          if [ "${{ github.event_name }}" = "schedule" ]; then
            if [ "${{ github.event.schedule }}" = "55 14 * * *" ]; then
              SEND="true"
            fi
          fi
          echo "send=$SEND" >> $GITHUB_OUTPUT

      - name: Run Radar
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          SEND_TELEGRAM: ${{ steps.decide.outputs.send }}
        run: |
          python -m radar.run

      - name: Commit outputs (out/daily + state.json)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add out/daily state.json
          if git diff --cached --quiet; then
            echo "No changes."
          else
            git commit -m "Radar: daily update"
            git push
          fi
